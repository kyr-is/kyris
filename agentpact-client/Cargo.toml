# SPDX-FileCopyrightText: Copyright 2026 Kyris
# SPDX-License-Identifier: Apache-2.0
[package]
name = "kyris-agentpact-client"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "AgentPact daemon UDS client: socket I/O, retry, daemon-state fallback"

[dependencies]
kyris-core = { workspace = true }
# Wire value types shared with agentpact's server crate (Mode et al.).
# Re-exported so out-of-tree consumers can branch on `Mode` without
# pulling in agentpact's policy engine.
agentpact-types = { workspace = true }
serde_json = { workspace = true }
uuid = { workspace = true }
